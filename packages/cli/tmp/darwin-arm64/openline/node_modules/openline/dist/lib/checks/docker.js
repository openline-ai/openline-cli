"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.installDocker = exports.dockerCheck = void 0;
const tslib_1 = require("tslib");
const shell = tslib_1.__importStar(require("shelljs"));
const dev_1 = require("../../config/dev");
function dockerCheck() {
    let result = shell.exec('which docker', { silent: true });
    if (result.code == 0) {
        return true;
    }
    else {
        return false;
    }
}
exports.dockerCheck = dockerCheck;
function installDocker() {
    let config = (0, dev_1.getConfig)();
    let result = shell.exec(config.dependencies.dockerMac);
    if (result.code == 0) {
        return true;
    }
    else {
        return false;
    }
}
exports.installDocker = installDocker;
